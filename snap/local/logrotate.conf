# Rotate the EPA Orchestrator state file daily, keeping a single backup.
# Environment variables are not expanded in logrotate configs, so use
# the absolute SNAP_DATA path for this snap.
/var/snap/epa-orchestrator/current/data/state.json {
    daily
    rotate 1
    copy
    missingok
    notifempty
}


